import {app} from './webapp'

describe('webapp', function () {
  it ('creates a GET route using .get', function () {
    assert.fail()
  })

  it ('creates a POST route using .get', function () {
    assert.fail()
  })

  it ('creates a POST AND GET route using .use', function () {
    assert.fail()
  })
})